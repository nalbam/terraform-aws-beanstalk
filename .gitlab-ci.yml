# gitlab-ci

image:
  name: hashicorp/terraform:light
  entrypoint:
    - '/usr/bin/env'
    - 'PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin'

variables:
  PLAN: plan.tfplan

stages:
  - plan

before_script:
  - terraform --version
  - terraform init

plan:
  stage: plan
  script:
    - terraform validate
    - terraform plan -out=$PLAN -var-file=sample.tfvars

after_script:
  - ls -alh
